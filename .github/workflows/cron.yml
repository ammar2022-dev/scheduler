name: Blurt Post Publisher

on:
  workflow_dispatch:  # Sirf manual trigger ke liye rakha hai

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Manual Publish
        run: |
          echo "Manual trigger only - cron-job.org handles automatic publishing"